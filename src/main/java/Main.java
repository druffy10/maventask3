import ru.netology.maventask.services.StatsService;

public class Main {
    public static void main(String[] args) {
/*        StatsService service = new StatsService();
        int[] sales = {
                8, 15, 13, 15, 17, 20, 19, 20, 7, 14, 14, 18
        };
        int totalSales = service.getTotalSales(sales);
        double averageSales = service.getAverageSales(sales);
        int maxMonth = service.getSalesMaxMonth(sales);
        int minMonth = service.getSalesMinMonth(sales);
        int monthsBelowAverageSales = service.getMonthsBelowAverageSales(sales);
        int monthsAboveAverageSales = service.getMonthsAboveAverageSales(sales);

        System.out.println("Сумма всех продаж: " + totalSales);
        System.out.println("Средняя сумма продаж в месяц: " + averageSales);
        System.out.println("Номер месяца с максимальными продажами: " + maxMonth);
        System.out.println("Номер месяца с минимальными продажами: " + minMonth);
        System.out.println("Количество месяцев, в которых продажи были ниже среднего: " + monthsBelowAverageSales);
        System.out.println("Количество месяцев, в которых продажи были выше среднего: " + monthsAboveAverageSales);
    }

 */
    }
}